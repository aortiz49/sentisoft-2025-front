{
    "compilerOptions": {
        "baseUrl": ".",
        "strict": true, // Enables strict type-checking options
        "module": "ES2020", // Specifies the module system to use. ES2020 modules are modern JavaScript modules (ESM)
        "target": "ES2020", // Specifies the target version of JavaScript (modern ECMAScript 2020)
        "moduleResolution": "Node", // Ensures module resolution works the same way as Node.js (with file extensions)
        "lib": ["DOM", "DOM.Iterable", "ESNext"], // Includes libraries for the DOM, modern iterable features, and the latest ECMAScript features
        "types": ["vite/client", "node"],
        "jsx": "react-jsx", // Enables the new JSX transform introduced in React 17. This basically makes it so we don't have to import React from 'react' in every file to use JSX
        "skipLibCheck": true, // Skips type checking of declaration files (improves build speed)
        "esModuleInterop": true, // Ensures compatibility between CommonJS and ESModule imports
        "paths": {
            "#/*": ["./*"] // Custom path mapping to import modules using `#/*` alias
        }
    },
    "include": ["**/*"],
    "exclude": ["node_modules"]
}
